{
  "name": "static-project-starter",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "dev": "rm -rf dist && parcel --no-cache",
    "build": "rm -rf dist && parcel build --no-cache --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maxackerman/static-project-starter.git"
  },
  "author": "Max Ackerman",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/maxackerman/static-project-starter/issues"
  },
  "homepage": "https://github.com/maxackerman/static-project-starter#readme",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3",
    "parcel": "^2.8.3"
  }
}
